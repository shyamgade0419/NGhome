/**
 * Resident tab entry for My Bills.
 *
 * The implementation lives at (app)/my-bills.tsx so admins — who are residents
 * too — can reach the same screen from their Settings menu without duplicating it.
 */
export { default } from '../my-bills';
